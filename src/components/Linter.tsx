import * as React from 'react'

const Linter: React.FC = () => {
    return (
        <div>
            When you start with legacy app you should start with setuping linter if it is not there
        </div>
    )
}

export default Linter
